This GitHub import keeps the project source, notebooks, reports, and helper scripts.

The following directories are intentionally excluded from version control in this repo snapshot:

- `projects/*/data/`
- `projects/*/outputs/`

They remain in the original local workspace and can be restored or uploaded separately if needed.
