# Figure 2: Brain Clustering and Heatmap

This folder contains the core motif-counting utilities used to compute motif counts, normalized motif frequencies, and ER-based motif Z-scores from a directed adjacency matrix.

Run:

```bash
python figure_02_brain_clustering_heatmap/fig2_core_clustering_heatmap.py
```
