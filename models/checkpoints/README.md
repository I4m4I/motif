# Model Checkpoints

Destination for trained model weights. No pretrained checkpoint files were included in
the original release; this directory is reserved for checkpoints produced by the training
entry points in [`training/`](../../training/) or supplied separately.

Checkpoint files are **not** versioned in Git (see [`.gitignore`](../../.gitignore)).
